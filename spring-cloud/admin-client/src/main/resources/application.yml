spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://127.0.0.1:9301
server:
  port: 9305
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
logging:
  file: admin-client.log